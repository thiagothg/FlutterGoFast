import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_gofast/app/modules/intro/intro_controller.dart';
import 'package:flutter_gofast/app/modules/intro/intro_module.dart';

void main() {
  initModule(IntroModule());
  // IntroController intro;
  //
  setUp(() {
    //     intro = IntroModule.to.get<IntroController>();
  });

  group('IntroController Test', () {
    //   test("First Test", () {
    //     expect(intro, isInstanceOf<IntroController>());
    //   });

    //   test("Set Value", () {
    //     expect(intro.value, equals(0));
    //     intro.increment();
    //     expect(intro.value, equals(1));
    //   });
  });
}
